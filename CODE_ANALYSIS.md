# Analyze Telegram React code

### `TelegramApp`

* `componentDidMount()` init with location
* Load `MainPage`
* L330: if not logged in, load `AuthForm`, pass `authorizationState`
* `authorizationState` is within `ApplicationStore`

### `MainPage`

### `Dialogs` component

